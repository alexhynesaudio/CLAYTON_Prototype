using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerViewController : MonoBehaviour
{
    //Vector2 mouseLook;
    //Vector2 smoothV;
    //public float sensitivity = 5.0f;
    //public float smoothing = 2.0f;
    //Camera player;
    //GameObject centre_Point;

    //float m__angle_yaw;
    //float m__angle_pitch;
    //float m__angle_roll;


    // Start is called before the first frame update
    void Start()
    {
        //player = this.transform.GetComponentInChildren<Camera>();
        //centre_Point = this.transform.gameObject;
        //m__angle_roll = 0.0f;
    }

    // Update is called once per frame
    void Update()
    {
        //m__angle_yaw = Input.mousePosition.x * smoothing;
        //m__angle_pitch = Input.mousePosition.y * smoothing;

        //centre_Point.transform.rotation.Set(m__angle_yaw, m__angle_pitch, m__angle_roll, 0.0f);




    }
}
